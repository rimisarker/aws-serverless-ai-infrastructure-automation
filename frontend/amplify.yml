version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist # যদি Vite ব্যবহার করেন তবে 'dist', React হলে 'build'
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
